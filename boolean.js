// // Задача 4.3: Создай файл boolean.js и сделай всё по аналогии с предыдущим заданием.

// // Приведи также пример, когда значения true или false не указываются напрямую, а вычисляются (см. операторы сравнения).

// // В конец файла перенеси следующий код и дополни его подходящими значениями (true или false).

// // Чтобы узнать какое значение подходит отталкивайся от названий переменных (может пригодиться переводчик).


// /*Логический тип Boolean в JavaScript представляет одно из двух значений: true (истина) или false (ложь).*

let i = (10 > 9) true;
let y = (10 == 9) false;

const iAmAStudent = true;
const isSpring = false;
const javaSciptIsBeauty = true;
const constCanBeChanged = false;
const letCanBeChanged = true;
const nullIsADataType = true;
const nullIsAValue = false;
const iAmFromGrozny = false;